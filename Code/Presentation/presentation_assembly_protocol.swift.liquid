{% include 'header' %}

import Foundation

protocol PresentationAssemblyProtocol {
    
    var router: AppRouterProtocol! { get }
    var whisper: InAppNotificationsProvider! { get }
    var socialService: SocialProvider! { get }
    var styleKit: StyleKit! { get }

}
